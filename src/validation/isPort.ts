export function isPort(value:string|number):boolean{if(typeof value==='number')return Number.isInteger(value)&&value>=0&&value<=65535;if(typeof value!=='string'||value.length===0)return false;if(!/^\d+$/.test(value))return false;if(value.length>1&&value[0]==='0')return false;const n=Number.parseInt(value,10);return n>=0&&n<=65535}