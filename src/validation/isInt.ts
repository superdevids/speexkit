export interface IsIntOptions{min?:number;max?:number;allowLeadingZero?:boolean}export function isInt(value:string,options?:IsIntOptions):boolean{if(typeof value!=='string'||value.length===0)return false;const{min,max,allowLeadingZero=false}=options??{};const re=allowLeadingZero?/^[+-]?\d+$/:/^[+-]?(?:0|[1-9]\d*)$/;if(!re.test(value))return false;const num=Number.parseInt(value,10);if(Number.isNaN(num))return false;if(min!==undefined&&num<min)return false;if(max!==undefined&&num>max)return false;return true}