export interface IsFloatOptions{min?:number;max?:number}const FLOAT_RE=/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)(?:[eE][+-]?\d+)?$/;export function isFloat(value:string,options?:IsFloatOptions):boolean{if(typeof value!=='string'||value.length===0)return false;if(!FLOAT_RE.test(value))return false;const num=Number.parseFloat(value);if(!Number.isFinite(num))return false;const{min,max}=options??{};if(min!==undefined&&num<min)return false;if(max!==undefined&&num>max)return false;return true}